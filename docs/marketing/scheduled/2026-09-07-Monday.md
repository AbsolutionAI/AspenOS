---
slot: mon-2026w37
pillar: ai-test-craft
theme: week-2-prove-before-ship
cta: none
status: draft
created: 2026-09-07
---

# Mon Sep 7 — The eval gate that runs before agents act

## DRAFT

Operator note: the eval gate that runs before agents act.

Every dispatch in our fleet passes through a lightweight CE gate before the tool call executes. One extra prompt: "verify your output schema and budget impact before handing to the executor."

Cost: ~150ms per call. Catches: hallucinated tool arguments, wrong identifiers, out-of-budget allocs.

We added it after an agent archived a full Linear project instead of a single issue. The error was one boolean. The recovery was a morning.

The gate is now non-negotiable in every agent's dispatch path.

## Rationale

- **Monday slot:** Bridges agent fleet ops (dispatch path) and AI build/test craft (eval gate) — both eligible for Monday per strategy §8
- **Week 2 theme (Prove Before Ship):** The CE gate is the literal "prove it before you ship it" mechanism — verify output schema and budget before the tool call leaves the agent
- **Operator note format:** 7 lines, one usable idea (add a pre-dispatch eval gate to every agent's tool path)
- **First-party proof:** real incident (agent archived a Linear project instead of one issue), real countermeasure (CE gate in dispatch path)
- **No CTA:** Monday rule per strategy
- **Voice:** calm, precise, systems-minded — no hashtags, no emoji chains, no engagement bait
- **Pillar health:** This is pillar 2 (AI build & test craft) at ~20% target; pillars 1–3 remain above 50% combined
- **Content pipeline note:** Execution is paused per Captain 2026-09-01 (BEL-155 Backlog). Draft stored for review; human gate remains.

## Approve protocol
Human: reply `post it`, `skip`, or `revise: …`