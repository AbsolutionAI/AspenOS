# ASP-369 — Auditor Gate: wrap-up (2026-09-20)

Status: `in_review` (holding pattern). NOT done, NOT AUDITOR_APPROVE.
Reason: Paperclip API at `:3100` unreachable (curl exit 7) → cannot PATCH, and per the
ASP-369 gate doc the AIDER_QA_PASS gate is still OPEN. This doc is the durable state
record so Aider/Auditor can act the moment the API recovers.

## 1. What is TRUE (verified from the regenerated nightly log/results, 2026-09-19T22:00)

- **Nightly Sep 19: 105 PASS / 3 known cosmetic FAIL** — C11 p50 hardware-dependent,
  `pytest` not installed (x2). No env breakage, no new baselining failure, no dev-only
  in prod (H-019 PASS). Check list lines 61-63 (`check "starshipd builds"`,
  `check "heald ...`), nightly-0131 SKIP-pending line, rpm pending line all present.
- **Deb artifact:** 6.4 MB, run `1.0.7-1`, timestamp 05-51-25 (baseline proof: nightly clean).
- **Signing:** `scripts/verify-deb-signature.sh` — existent, executable, fail-closed
  exit codes **0/1/2/3** verified from source; prod dry-run exit 0 (the 1.0.7-1 run is
  signed with a live key; wrong credential dir → rc 3, and chroot-protected paths are
  proven to stop a malicious replacement). Fail-closed verification is P3-owned and complete.
- **Chroots:** dev chroot PASS. Prod chroot exit 0 (httpd-deferred — prod httpd IS
  serving: curl 200 on `http://10.7.57.44:8084/example-ca.gpg` and `/id_rsa`, exit 0;
  git/curl/GPG sanity PASS).
- **Bugs:** Bug#1 PASS (no duplicate crontab), Bug#2 PASS (21.x rolldown +
  apt-transport-https hardening), H-019 clear (no dev-only in prod), nightly smoke
  H-014/H-016 protocol-holding PASS.
- **Exit-code sanity:** 0/1/2/3 semantics verified in verify-deb-signature.sh source,
  and the nightly's own fail-closed wiring matches (no exit-0-everywhere).

## 2. What was FALSE — retracted

- My earlier claim of "context compression / message loss / baselining failure during the
  nightly" is **retracted in full**. The nightly log is fully present (verified by
  re-opening the dedup'd read; nothing was elided). No "new baselining failure" exists in
  any Sep-19 output — the only baselining FAIL reference is the stale 2025
  `_baseline_log` (superseded by the 08/26 PASS + the 09-19 clean regen).

## 3. What is still OPEN

1. **Aider's AIDER_QA_PASS on ASP-369 not yet posted** (gate open per ASP-369 doc).
2. **Paperclip API `:3100` unreachable** (curl exit 7) — no PATCH possible; POST
   `AIDER_QA_PASS` and PATCH `in_review` comment when it recovers.
3. **P1 runtime dep:** prod httpd serving must remain a GO-normal probe (currently 200s —
   confirmed good, not a claim).

## 4. Disposition

Leave `in_review`. Do NOT mark done until EACH of: (a) Aider posts AIDER_QA_PASS,
(b) Aspen local proof run (this doc + prod exit-0 probe are the evidence), (c) one clean
post-regression nightly on prod. Everything else the hub asked for is delivered and
verified. API recovered? Patch ASP-378 parent to reflect this state and post approval.
