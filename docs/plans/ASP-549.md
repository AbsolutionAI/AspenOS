# ASP-549: Nightly Packaging & Deployment Check

## Summary

Run the nightly packaging & deployment check on the control plane host, record results, and fix any issues found.

## Background

This is the recurring nightly packaging & deployment check for Aspen OS v2.2.0. The check runs in 15 sections covering Go/Rust/C11 builds, smoke tests, Debian/Windows packaging, systemd units, shell syntax, static inventory, Python test suite, ISO structure, and dashboard assets.

The previous iteration (ASP-546) found and fixed the shell syntax check scope (`packaging/*.sh` was not included). Results showed 101 passed, 1 known failure (C11 p50 benchmark deviation).

## Scope

1. Run `bash scripts/check-nightly.sh` — automated check in 15 sections
2. Record pass/fail results with section breakdown
3. Fix any new failures (unrelated to the known C11 p50 benchmark)
4. Update baseline deviations if any check counts changed
5. Write compound learning

## Items

| # | Item | Expected action |
|---|------|----------------|
| 1 | Run check-nightly.sh | Execute and record output |
| 2 | Diagnose failures | Fix or document as known |
| 3 | Record results | Write `docs/nightly-check-results-2026-09-03.md` |
| 4 | Compound learning | Write `docs/solutions/asp-549-nightly-check.md` |

## Verification

- `bash scripts/check-nightly.sh` exits with expected pass/fail counts
- Results doc accurately reflects the run output
- Compound learning captures any new findings