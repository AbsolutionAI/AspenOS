# ASP-524: Nightly Packaging & Deployment Check — Improvements

## Problem

The nightly check (`scripts/check-nightly.sh`) runs clean on this control-plane host:
**78 passed, 1 failed** (the known C11 p50 benchmark deviation). However, three
improvements are warranted:

| # | Issue | Detail |
|---|-------|--------|
| 1 | nats-server version drift | `nightly.yml` and `ci.yml` install v2.14.3; baseline doc and local host use v2.14.5 |
| 2 | iso-smoke baseline stale | Ops doc lists `make iso-smoke` as a separate 32-test run; it is now integrated into `scripts/smoke-test.sh` (58 total tests) |
| 3 | Gatekeeper module unchecked | New `src/python/gatekeeper/minimal_shim.py` exists but no nightly check verifies it |

## Fixes

### 1. nats-server v2.14.3 → v2.14.5

**Files:** `.github/workflows/nightly.yml`, `.github/workflows/ci.yml`
**Change:** Update the nats-server download URL from v2.14.3 to v2.14.5 in both
workflow files.
**Rationale:** The baseline doc already records v2.14.5 as the reference
version. The local environment has v2.14.5. CI runners should match.

### 2. Baseline doc update

**File:** `docs/ops/NIGHTLY_PACKAGING_DEPLOY_CHECK.md`
**Changes:**
- Merge iso-smoke row into the smoke-test row: the combined `scripts/smoke-test.sh`
  suite runs 58 tests (including iso-firstboot-smoke). Remove the separate
  `make iso-smoke` row since it is no longer an independent make target in the
  nightly flow.
- Add "nats-server version" to the baseline table for clarity.
- Add a new row for the gatekeeper module check (Section 12).

### 3. Gatekeeper module check

**File:** `scripts/check-nightly.sh`
**Change:** Add Section 12 — Gatekeeper module — with checks for:
- `src/python/gatekeeper/minimal_shim.py` exists
- The file is valid Python (syntax check)

## Files changed

- `.github/workflows/nightly.yml` — nats-server v2.14.5
- `.github/workflows/ci.yml` — nats-server v2.14.5
- `docs/ops/NIGHTLY_PACKAGING_DEPLOY_CHECK.md` — baseline update, gatekeeper row
- `scripts/check-nightly.sh` — Section 12 gatekeeper check

## Verification (2026-08-30 run)

All three plan items confirmed already deployed. Nightly check executed clean:

| Metric | Value |
|--------|-------|
| Passed | 80 |
| Failed | 1 (known C11 p50 benchmark deviation) |
| Total  | 81 |
| Time   | 11,618ms |

**Item 1:** Both workflow files use `v2.14.5`; host nats-server also on `v2.14.5`.
**Item 2:** Baseline doc has merged smoke row (58 tests), nats-server version row, gatekeeper row.
**Item 3:** Section 12 present in `check-nightly.sh` with gatekeeper shim existence + syntax checks.

No remaining work.